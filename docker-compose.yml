version: "3.9"

services:
  api:
    build: .
    ports:
      - "8006:8006"
    environment:
      - ENV=development
      - STRIPE_API_KEY=${STRIPE_API_KEY:-}
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8006 --reload
